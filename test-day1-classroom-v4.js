var fs=require('fs'),s=fs.readFileSync('day1/classroom-v4.js','utf8'),p=0,f=0;function ok(n,c){if(c){console.log('PASS '+n);p++}else{console.log('FAIL '+n);f++}}ok('six math areas',['logs','algebra','exponents','scientific_notation','fractions_percent','unit_conversions'].every(function(x){return s.indexOf(x+':{name:')>=0}));ok('previous problem',s.indexOf('Previous problem')>=0&&s.indexOf('historyIndex--')>=0);ok('wrong answer retries same problem',s.indexOf('Retry this same problem')>=0);ok('skip remains same skill',s.indexOf('fresh '+"'+esc(a.name)+'"+' problem')>=0||s.indexOf('Skip this problem')>=0);ok('fraction common denominator teaching',s.indexOf('common denominator')>=0);ok('fraction of number teaching',s.indexOf('Divide by the denominator, then multiply by the numerator')>=0);ok('all math skills menu',s.indexOf('All math skills')>=0);console.log(p+' passed, '+f+' failed');if(f)process.exit(1);