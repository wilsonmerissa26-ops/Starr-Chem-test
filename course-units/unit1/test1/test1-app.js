(function(){'use strict';var D=globalThis.Test1AdaptiveData,E=globalThis.Test1AdaptiveEngine,S=globalThis.Test1AdaptiveSupport,R=globalThis.StudentModelIdkRouter;if(!D||!E||!S||!R)throw new Error('Test 1 tutor dependencies missing');var cards=document.querySelector('[data-t1-cards]'),host=document.querySelector('[data-t1-host]'),active=null,message='';var PH={probe:'Quick Diagnostic',watch:'Watch · I Do',concept:'Supported Concept Check',build:'Build Together · We Do',guided:'Guided Practice',independent:'Cold Independent',explain:'Explain Why',transfer:'Transfer',activity:'Different Chemistry Activity','retrieval-wait':'Later Retrieval',retrieval:'Later Retrieval · Cold',complete:'Mastered'};
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c];});}
function key(id){return'chm221.test1.'+id+'.v1';}function save(){if(active)localStorage.setItem(key(active.lessonId),JSON.stringify(active));renderCards();}function load(id){try{var x=localStorage.getItem(key(id));return x?JSON.parse(x):null;}catch(_){return null;}}function lesson(){return active?D.lesson(active.lessonId):null;}function status(id){var s=load(id);if(!s)return'Not started';if(s.mastered||s.phase==='complete')return'Mastered';return'In progress';}
function renderCards(){var ext=D.EXTERNAL.map(function(x){return'<article class="skill-card foundation"><div class="eyebrow">Foundation repair route</div><h3>'+esc(x.title)+'</h3><p>'+esc(x.note)+'</p><a class="button-link" href="'+esc(x.href)+'">Open full foundation lesson</a></article>';}).join('');var own=D.all().map(function(l){var st=status(l.id);return'<article class="skill-card '+(active&&active.lessonId===l.id?'selected':'')+'"><div class="eyebrow">Priority '+l.priority+' · '+esc(l.chapter)+'</div><span class="status '+st.toLowerCase().replace(/\s/g,'-')+'">'+esc(st)+'</span><h3>'+esc(l.title)+'</h3><p>'+esc(l.objective)+'</p><button type="button" data-open="'+esc(l.id)+'">'+(st==='Not started'?'Check this skill':'Continue')+'</button></article>';}).join('');cards.innerHTML=ext+own;cards.querySelectorAll('[data-open]').forEach(function(b){b.addEventListener('click',function(){active=load(b.dataset.open)||E.createSession(b.dataset.open,Date.now());message='';save();render();history.replaceState(null,'','?skill='+encodeURIComponent(active.lessonId));});});}
function answers(form){var out={};new FormData(form).forEach(function(v,k){out[k]=v;});return out;}function fieldForm(item,label,help){return'<form data-answer class="answer-form">'+item.fields.map(function(f){return'<label>'+esc(f.label)+'<input name="'+esc(f.id)+'" autocomplete="off" inputmode="text"></label>';}).join('')+'<div class="actions"><button type="submit">'+esc(label||'Check')+'</button>'+(help?'<button type="button" class="ghost" data-help>I don’t know yet</button>':'')+'</div></form>';}
function visual(v){if(!v)return'';return'<div class="visual-stage"><strong>'+esc(v)+'</strong></div>';}
function feedback(){return message?'<div class="feedback" role="status">'+esc(message)+'</div>':'';}
function shell(inner){var l=lesson();return'<section class="lesson-shell"><div class="lesson-head"><div><div class="eyebrow">Test 1 adaptive tutor · '+esc(l.chapter)+'</div><h2>'+esc(l.title)+'</h2></div><span class="phase-pill" id="phaseLabel">'+esc(PH[active.phase]||active.phase)+'</span></div>'+feedback()+inner+'</section>';}
function bindForm(fn){var f=host.querySelector('[data-answer]');if(f)f.addEventListener('submit',function(e){e.preventDefault();fn(answers(f));});}
function reasonGrid(cold){return'<div class="reason-grid">'+S.REASONS.map(function(r){return'<button type="button" class="reason" data-reason="'+esc(r.id)+'" data-cold="'+(cold?'1':'0')+'">'+esc(r.label)+'</button>';}).join('')+'</div>';}
function issuePanel(){if(!active.supportedIssue)return'';return'<div class="diagnosis-card"><div class="eyebrow">Dr. Merissa found the first broken step</div><h3>'+esc(S.label(active.supportedIssue.errorCode))+'</h3><p>I’m not going to make you keep guessing the same question. Tell me what kind of stuck this is so I can use the right repair.</p>'+reasonGrid(false)+'</div>';}
function bindIssue(){host.querySelectorAll('[data-reason]').forEach(function(b){b.addEventListener('click',function(){var reason=b.dataset.reason;if(b.dataset.cold==='1'&&active.repair){var rr=S.route(lesson(),active.repair.errorCode,reason,'');active.repair.reason=reason;active.repair.teaching=rr.text;active.repair.label=rr.label;active.repair.foundationHref=rr.foundationHref||null;message='';save();render();return;}var r=E.requestSupportedHelp(active,reason,Date.now());message='';save();render();});});}
function phaseBanner(){if(active.phase==='probe')return'<div class="probe-banner"><b>Quick diagnostic, not mastery.</b><br>Two clean answers let you skip the long teaching sequence. A miss tells the tutor exactly what to teach.</div>';if(active.phase==='concept')return'<div class="supported-banner"><b>Supported Concept Check</b><br>Low-risk check. Wrong answers become teaching data, not dead ends.</div>';if(active.phase==='build')return'<div class="build-banner"><b>Build Together · We Do</b><br>One decision at a time. We stop on the exact decision that breaks.</div>';if(active.phase==='guided')return'<div class="guided-banner"><b>Guided Practice</b><br>You do the problem. Support is available, but I only step in where the reasoning fails.</div>';if(active.phase==='transfer')return'<div class="transfer-banner"><b>Transfer</b><br>Same idea, different-looking problem. This does not create mastery evidence.</div>';return'';}
function renderProbe(){var item=E.currentItem(active);host.innerHTML=shell(phaseBanner()+'<div class="progress">Diagnostic '+(active.probeIndex+1)+' of '+lesson().probe.length+'</div><div class="prompt">'+esc(item.prompt)+'</div>'+fieldForm(item,'Check diagnostic',true)+issuePanel());bindForm(function(a){var r=E.submitProbe(active,a,Date.now());if(r.correct)message=r.testedOutOfTeaching?'You proved the entry skill twice. Skipping the long lesson and moving to cold confirmation.':'Good. One more diagnostic item.';else message='That answer exposed the '+S.label(r.errorCode)+' step. I can teach that instead of making you guess.';save();render();});var h=host.querySelector('[data-help]');if(h)h.addEventListener('click',function(){E.askSupportedHelp(active);message='Tell me what kind of stuck this is.';save();render();});bindIssue();}
function renderWatch(){var step=lesson().watch[active.watchIndex];host.innerHTML=shell('<div class="teacher"><div class="avatar">DM</div><div><b>Dr. Merissa</b><p>'+esc(step.text)+'</p></div></div>'+visual(step.visual)+'<div class="watch-check"><div class="eyebrow">Do something with the explanation</div><div class="prompt">'+esc(step.check.prompt)+'</div>'+fieldForm(step.check,'Check prediction',true)+'</div>'+issuePanel());bindForm(function(a){var r=E.submitWatchCheck(active,a,Date.now());message=r.correct?'Yes. That prediction follows from the rule.':'That prediction tells me where the reasoning broke: '+S.label(r.errorCode)+'.';save();render();});var h=host.querySelector('[data-help]');if(h)h.addEventListener('click',function(){E.askSupportedHelp(active);message='Choose what kind of help would actually help.';save();render();});bindIssue();}
function renderSupported(){var item=E.currentItem(active);host.innerHTML=shell(phaseBanner()+'<div class="prompt">'+esc(item.prompt)+'</div>'+fieldForm(item,active.phase==='build'?'Check this build step':'Check',true)+issuePanel());bindForm(function(a){var r=E.submitSupported(active,a,Date.now());message=r.correct?(active.phase==='activity'?'':'That step is solid. Moving forward.'):'I found the first broken step: '+S.label(r.errorCode)+'. Pick what is getting in your way and I’ll repair only that.';save();render();});var h=host.querySelector('[data-help]');if(h)h.addEventListener('click',function(){E.askSupportedHelp(active);message='No guessing. Tell me what kind of stuck this is.';save();render();});bindIssue();}
function renderRepair(){var rp=active.repair,check=rp.check;host.innerHTML=shell('<div class="repair-card"><div class="eyebrow">Targeted repair · '+esc(rp.label||S.label(rp.errorCode))+'</div><h3>We are fixing one thing, not restarting the whole chapter.</h3><div class="teacher"><div class="avatar">DM</div><div><b>Dr. Merissa</b><p>'+esc(rp.teaching)+'</p></div></div>'+(rp.foundationHref?'<a class="foundation-link" href="'+esc(rp.foundationHref)+'">Open the full prerequisite lesson</a>':'')+'<div class="support-switch"><b>Would a different kind of help make more sense?</b>'+reasonGrid(!!rp.cold)+'</div><div class="repair-check"><b>Prove the smaller idea before we return.</b><div class="prompt">'+esc(check.prompt)+'</div>'+fieldForm(check,'Check repair',false)+'</div></div>');bindForm(function(a){var r=rp.cold?E.submitColdRepair(active,a,Date.now()):E.submitSupportedRepair(active,a,Date.now());if(r.correct)message='Repair passed. You get a fresh problem, not the one you already saw.';else if(r.representationSwitched)message='Still shaky, so I changed the representation instead of repeating the same explanation.';else if(r.escalated)message='This foundation needs a full re-teach. We stepped back to Watch.';else message='That repair is not stable yet.';save();render();});bindIssue();}
function renderIndependent(){if(active.repair&&active.repair.active)return renderRepair();var item=E.currentItem(active),st=E.policy(active);host.innerHTML=shell('<div class="cold-banner"><b>Cold evidence</b><br>No hints or notes if you want this item to count. Help is always available, but using it contaminates this item and forces a fresh one.</div><div class="evidence">Clean cold items: '+st.cleanCount+'/'+st.minClean+(st.requiredTags.length?' · Required coverage: '+esc(st.requiredTags.join(', ')):'')+'</div><div class="prompt">'+esc(item.prompt)+'</div>'+fieldForm(item,'Check cold answer',true));bindForm(function(a){var r=E.submitIndependent(active,a,Date.now());if(r.correct)message=r.sufficientEvidence?'Cold evidence is sufficient. Now explain why.':'Correct cold item. More coverage is needed before explanation.';else message='That miss is diagnostic. This item is contaminated, so we repair the exact step and then use a fresh item.';save();render();});host.querySelector('[data-help]').addEventListener('click',function(){var code=item.fields[0].errorCode;active.currentColdItemId=item.id;active.currentColdContaminated=true;var rr=E.requestColdHelp(active,S.REASONS[0].id,Date.now());message='This item is now supported. Choose the kind of help that fits you best.';save();render();});}
function renderExplain(){host.innerHTML=shell('<div class="cold-banner"><b>Your reasoning is evidence.</b><br>Explain the mechanism of the answer, not just the final label.</div><div class="prompt">'+esc(lesson().explanation.prompt)+'</div><form data-explain><textarea name="x" rows="6" required></textarea><div class="actions"><button type="submit">Check explanation</button></div></form>');host.querySelector('[data-explain]').addEventListener('submit',function(e){e.preventDefault();var t=new FormData(e.currentTarget).get('x'),r=E.submitExplanation(active,t,Date.now());message=r.correct?'Explanation attached. Now prove the skill transfers.':'The explanation does not yet prove the reasoning. We are repairing the missing foundation and then using a fresh cold item.';save();render();});}
function renderActivity(){var item=E.currentItem(active);host.innerHTML=shell('<div class="activity-banner"><b>Switch chemistry on purpose.</b><br>This creates meaningful spacing. It does not add mastery evidence for '+esc(lesson().title)+'.</div><div class="prompt">'+esc(item.prompt)+'</div>'+fieldForm(item,'Check activity',false));bindForm(function(a){var r=E.submitActivity(active,a,Date.now());message=r.correct?'Different chemistry activity complete. Retrieval still waits for the minimum interval.':r.teaching;save();render();});}
function waitText(ms){var s=Math.ceil(ms/1000);return s<60?s+' seconds':Math.ceil(s/60)+' minutes';}function renderWait(){var r=E.retrievalReadiness(active,Date.now());host.innerHTML=shell('<div class="retrieval-card"><h3>Later means later.</h3><p>Both conditions are required: a different chemistry activity and the shared minimum retrieval interval.</p><p><b>Activity:</b> '+(active.activityComplete?'complete':'not complete')+'</p><p><b>Interval:</b> '+(r.ready?'ready':r.remainingMs!=null?waitText(r.remainingMs)+' remaining':'not ready')+'</p><button type="button" id="ready">'+(r.ready?'Start later retrieval':'Check readiness')+'</button></div>');host.querySelector('#ready').addEventListener('click',function(){var x=E.startRetrieval(active,Date.now());message=x.accepted?'Retrieval unlocked. Answer from memory.':'Not ready yet: '+waitText(x.remainingMs||0)+' remaining.';save();render();});}
function renderRetrieval(){if(active.repair&&active.repair.active)return renderRepair();var item=E.currentItem(active);host.innerHTML=shell('<div class="cold-banner"><b>Later cold retrieval</b><br>This is the distinct delayed evidence opportunity. Help contaminates it.</div><div class="prompt">'+esc(item.prompt)+'</div>'+fieldForm(item,'Check retrieval',true));bindForm(function(a){var r=E.submitRetrieval(active,a,Date.now());if(r.mastery&&r.mastery.mastered)message='Mastery confirmed by the shared Student Model.';else if(r.correct)message='Correct retrieval recorded, but mastery is not yet valid.';else message='Retrieval miss found a gap. Repair it, then use a fresh retrieval item.';save();render();});host.querySelector('[data-help]').addEventListener('click',function(){E.requestColdHelp(active,S.REASONS[0].id,Date.now());message='Retrieval help opened. This item no longer counts as cold evidence.';save();render();});}
function renderComplete(){host.innerHTML=shell('<div class="complete"><div class="checkmark">✓</div><h2>Skill mastered</h2><p>The shared Student Model found valid explained cold evidence and a different delayed cold success after meaningful intervening chemistry.</p><button type="button" id="back">Back to Test 1 skills</button></div>');host.querySelector('#back').addEventListener('click',function(){active=null;message='';render();});}
function overview(){host.innerHTML='<section class="overview"><div class="eyebrow">How this tutor works</div><h2>Prove what you know. Teach only what breaks.</h2><p>Start with the Chapter 4 priorities. Each skill begins with a short diagnostic. Strong skills fast-track. Weak skills open the full Dr. Merissa teaching loop, and prerequisite gaps can send you backward to foundation repair before returning.</p><div class="flow">Diagnostic → Teach if needed → Build Together → Guided → Cold → Explain → Transfer → Later Retrieval</div><p class="small">Historical Test 1 evidence is used to make the practice realistic, but the 2026 syllabus remains the scope authority.</p></section>';}
function render(){renderCards();if(!active){overview();return;}if(active.repair&&active.repair.active)return renderRepair();if(active.phase==='probe')renderProbe();else if(active.phase==='watch')renderWatch();else if(['concept','build','guided','transfer'].indexOf(active.phase)!==-1)renderSupported();else if(active.phase==='independent')renderIndependent();else if(active.phase==='explain')renderExplain();else if(active.phase==='activity')renderActivity();else if(active.phase==='retrieval-wait')renderWait();else if(active.phase==='retrieval')renderRetrieval();else if(active.phase==='complete')renderComplete();else overview();}
var qv=new URLSearchParams(location.search).get('skill');if(qv&&D.lesson(qv))active=load(qv)||E.createSession(qv,Date.now());render();})();